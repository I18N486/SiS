<?xml version="1.0" encoding="utf-8"?><!-- 按钮的选择器，可以设置按钮在不同状态下的时候，按钮不同的颜色 -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_checked="true">
        <shape
            android:shape="rectangle">
            <corners android:radius="20dp" />
            <size
                android:width="80dp"
                android:height="40dp"/>
            <stroke
                android:width="5dp"
                android:color="@color/transparent" />
            <solid android:color="@color/color_498eff" />
        </shape>
    </item>
    <item>
        <shape
            android:shape="rectangle">
            <stroke
                android:width="5dp"
                android:color="@color/transparent" />
            <size
                android:width="80dp"
                android:height="40dp"/>
            <corners android:radius="20dp" />
            <solid android:color="@color/color_707070" />
        </shape>
    </item>
</selector>
